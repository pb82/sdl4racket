Must rewrite events so that users can create individual events and
push them onto the queue (sdl-push-event)

Must get http://www.libsdl.org/projects/SDL_image/

Must figure out Mac OS X dynamic loading

Must test Win32, Win64, Lin32, Lin64.
